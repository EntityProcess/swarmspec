---
import type { Props } from '@astrojs/starlight/props';

const { data } = Astro.props;
---

<div class="lander-wrapper">
<div class="lander">
  <section class="hero">
    <div class="hero-content">
      <div class="hero-badge">Open Source Protocol</div>
      <h1 class="hero-title">Your agents don't need<br/>more power.<br/>They need a process.</h1>
      <p class="hero-tagline">
        HiveSpec is a spec-driven delivery lifecycle for AI agent swarms.
        Each agent follows the same disciplined phases — from claiming an issue to shipping a verified PR.
      </p>
      <div class="hero-actions">
        <a href="/docs/" class="button primary">
          Get Started
          <svg viewBox="0 0 20 20" fill="currentColor" width="16" height="16">
            <path fill-rule="evenodd" d="M3 10a.75.75 0 01.75-.75h10.638L10.23 5.29a.75.75 0 111.04-1.08l5.5 5.25a.75.75 0 010 1.08l-5.5 5.25a.75.75 0 11-1.04-1.08l4.158-3.96H3.75A.75.75 0 013 10z" clip-rule="evenodd" />
          </svg>
        </a>
        <a href="https://github.com/EntityProcess/hivespec" class="button secondary" target="_blank" rel="noopener">
          <svg viewBox="0 0 24 24" fill="currentColor" width="18" height="18">
            <path d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z" />
          </svg>
          View on GitHub
        </a>
      </div>

      <div class="install-command">
        <span class="install-prompt">$</span>
        <code>claude plugin add EntityProcess/hivespec</code>
        <button class="copy-button" data-copy="claude plugin add EntityProcess/hivespec" title="Copy to clipboard">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="16" height="16">
            <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
            <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
          </svg>
        </button>
      </div>
    </div>
  </section>

  <section class="lifecycle">
    <div class="section-header">
      <span class="section-label">The Lifecycle</span>
      <h2 class="section-title">Spec. Build. Verify. Ship.</h2>
      <p class="section-desc">Every agent follows the same disciplined path from issue to merged PR</p>
    </div>

    <div class="phase-flow">
      <div class="phase-card">
        <div class="phase-number">1</div>
        <div class="phase-info">
          <h3 class="phase-name">Spec</h3>
          <p class="phase-desc">Claim the issue, explore the codebase, and write a design spec before touching code.</p>
        </div>
      </div>

      <div class="phase-card">
        <div class="phase-number">2</div>
        <div class="phase-info">
          <h3 class="phase-name">Build</h3>
          <p class="phase-desc">Break the spec into tasks. Test-driven implementation. Red, green, refactor, commit.</p>
        </div>
      </div>

      <div class="phase-card">
        <div class="phase-number">3</div>
        <div class="phase-info">
          <h3 class="phase-name">Verify</h3>
          <p class="phase-desc">Run real commands. Check blast radius. Evidence over assertions.</p>
        </div>
      </div>

      <div class="phase-card">
        <div class="phase-number">4</div>
        <div class="phase-info">
          <h3 class="phase-name">Ship</h3>
          <p class="phase-desc">Classify risk. Auto-merge safe changes. Flag breaking changes for review.</p>
        </div>
      </div>
    </div>

    <div class="phase-detail-link">
      <a href="/phases/claim/">See all 7 phases &rarr;</a>
    </div>
  </section>

  <section class="problem">
    <div class="section-header">
      <span class="section-label">The Problem</span>
      <h2 class="section-title">Agents ship fast. They don't ship right.</h2>
    </div>
    <div class="problem-grid">
      <div class="problem-card">
        <div class="problem-icon bad">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="20" height="20">
            <path d="M18 6L6 18M6 6l12 12" />
          </svg>
        </div>
        <h3>No coding without specs</h3>
        <p>Agents jump straight to code. Skip the design. Break three files to fix one.</p>
      </div>
      <div class="problem-card">
        <div class="problem-icon bad">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="20" height="20">
            <path d="M18 6L6 18M6 6l12 12" />
          </svg>
        </div>
        <h3>No guessing at impact</h3>
        <p>They modify a shared interface without checking who depends on it.</p>
      </div>
      <div class="problem-card">
        <div class="problem-icon bad">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="20" height="20">
            <path d="M18 6L6 18M6 6l12 12" />
          </svg>
        </div>
        <h3>No "tests pass" without proof</h3>
        <p>They claim everything works. No command output. No evidence.</p>
      </div>
      <div class="problem-card">
        <div class="problem-icon bad">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="20" height="20">
            <path d="M18 6L6 18M6 6l12 12" />
          </svg>
        </div>
        <h3>No blast radius check</h3>
        <p>They ship a PR without verifying consumers of modified code still work.</p>
      </div>
    </div>
  </section>

  <section class="disciplines">
    <div class="section-header">
      <span class="section-label">What HiveSpec Enforces</span>
      <h2 class="section-title">Every phase has a gate</h2>
    </div>
    <div class="discipline-grid">
      <div class="discipline-card">
        <div class="discipline-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="22" height="22">
            <path d="M9 12h6M9 16h6M13 3H6a2 2 0 00-2 2v14a2 2 0 002 2h12a2 2 0 002-2V8l-5-5z" />
            <path d="M13 3v5h5" />
          </svg>
        </div>
        <h3>Spec before code</h3>
        <p>hs-design blocks implementation until a design is written and approved.</p>
      </div>
      <div class="discipline-card">
        <div class="discipline-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="22" height="22">
            <circle cx="11" cy="11" r="8" />
            <path d="M21 21l-4.35-4.35" />
          </svg>
        </div>
        <h3>Explore before designing</h3>
        <p>hs-explore finds all consumers of shared interfaces before proposing changes.</p>
      </div>
      <div class="discipline-card">
        <div class="discipline-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="22" height="22">
            <path d="M14.7 6.3a1 1 0 000 1.4l1.6 1.6a1 1 0 001.4 0l3.77-3.77a6 6 0 01-7.94 7.94l-6.91 6.91a2.12 2.12 0 01-3-3l6.91-6.91a6 6 0 017.94-7.94l-3.76 3.76z" />
          </svg>
        </div>
        <h3>Red before green</h3>
        <p>hs-implement watches tests fail before writing implementation code.</p>
      </div>
      <div class="discipline-card">
        <div class="discipline-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="22" height="22">
            <path d="M9 11l3 3L22 4" />
            <path d="M21 12v7a2 2 0 01-2 2H5a2 2 0 01-2-2V5a2 2 0 012-2h11" />
          </svg>
        </div>
        <h3>Evidence before claims</h3>
        <p>hs-verify requires actual command output, not assertions.</p>
      </div>
      <div class="discipline-card">
        <div class="discipline-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="22" height="22">
            <circle cx="12" cy="12" r="10" />
            <path d="M12 8v4l3 3" />
          </svg>
        </div>
        <h3>Risk before merge</h3>
        <p>hs-ship classifies risk and requires confirmation for elevated-risk PRs.</p>
      </div>
      <div class="discipline-card">
        <div class="discipline-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="22" height="22">
            <path d="M17 21v-2a4 4 0 00-4-4H5a4 4 0 00-4-4v-2" />
            <circle cx="9" cy="7" r="4" />
            <path d="M23 21v-2a4 4 0 00-3-3.87M16 3.13a4 4 0 010 7.75" />
          </svg>
        </div>
        <h3>Swarm-ready</h3>
        <p>Ten agents running HiveSpec concurrently is a swarm that delivers like a team.</p>
      </div>
    </div>
  </section>

  <section class="compatibility">
    <div class="section-header">
      <span class="section-label">Compatibility</span>
      <h2 class="section-title">Works with any agent</h2>
      <p class="section-desc">SKILL.md files. No lock-in. Any agent that reads them can run HiveSpec.</p>
    </div>
    <div class="client-grid">
      <div class="client-item">Claude Code</div>
      <div class="client-item">GitHub Copilot</div>
      <div class="client-item">Cursor</div>
      <div class="client-item">Codex</div>
      <div class="client-item">Gemini CLI</div>
      <div class="client-item">Pi (GPT-5.1)</div>
    </div>
  </section>

  <section class="specs-anywhere">
    <div class="section-header">
      <span class="section-label">Flexible Specs</span>
      <h2 class="section-title">The spec can live anywhere</h2>
    </div>
    <div class="spec-options">
      <div class="spec-card">
        <code class="spec-path">issue body</code>
        <p>Lightweight. No extra files. The agent reads it during hs-claim.</p>
      </div>
      <div class="spec-card">
        <code class="spec-path">.agents/plans/</code>
        <p>Written during hs-design. Lives on the branch. Deleted after merge.</p>
      </div>
      <div class="spec-card">
        <code class="spec-path">specs/</code>
        <p>For repos that use <a href="https://openspec.dev" target="_blank" rel="noopener">OpenSpec</a> conventions. Fully compatible.</p>
      </div>
    </div>
  </section>

  <section class="cta">
    <div class="cta-content">
      <h2 class="cta-title">Give your agents discipline.</h2>
      <p class="cta-desc">Install HiveSpec in one command and start shipping with confidence.</p>
      <div class="cta-install">
        <span class="install-prompt">$</span>
        <code>claude plugin add EntityProcess/hivespec</code>
      </div>
      <div class="cta-actions">
        <a href="/docs/" class="button primary">Read the docs</a>
        <a href="https://github.com/EntityProcess/hivespec" class="button secondary" target="_blank" rel="noopener">GitHub</a>
      </div>
    </div>
  </section>

  <footer class="lander-footer">
    <div class="footer-content">
      <span class="footer-brand">HiveSpec</span>
      <span class="footer-sep">&middot;</span>
      <span class="footer-license">MIT License</span>
      <span class="footer-sep">&middot;</span>
      <a href="https://github.com/EntityProcess/hivespec" class="footer-link" target="_blank" rel="noopener">GitHub</a>
    </div>
  </footer>
</div>
</div>

<script>
  document.querySelectorAll('.copy-button').forEach((button) => {
    button.addEventListener('click', async () => {
      const text = button.getAttribute('data-copy');
      if (text) {
        await navigator.clipboard.writeText(text);
        button.classList.add('copied');
        setTimeout(() => button.classList.remove('copied'), 2000);
      }
    });
  });
</script>

<style>
  .lander-wrapper {
    --color-accent: #f59e0b;
    --color-accent-light: #fbbf24;
    --color-accent-dim: #d97706;
    --color-success: #10b981;
    --color-red: #ef4444;
    --color-cyan: #06b6d4;
    --surface-1: rgba(255, 255, 255, 0.03);
    --surface-2: rgba(255, 255, 255, 0.05);
    --surface-3: rgba(255, 255, 255, 0.08);
    --border-subtle: rgba(255, 255, 255, 0.08);
    --border-default: rgba(255, 255, 255, 0.12);
    --text-primary: #f8fafc;
    --text-secondary: #94a3b8;
    --text-muted: #64748b;
    --bg-base: #0a0a0f;
    background: var(--bg-base);
    color: var(--text-primary);
    position: relative;
    margin: 0 calc(-50vw + 50%);
    padding: 0 calc(50vw - 50%);
    overflow: hidden;
  }

  .lander-wrapper::before {
    content: '';
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 100%;
    max-width: 900px;
    height: 700px;
    background: radial-gradient(ellipse at 50% 0%, rgba(245, 158, 11, 0.1) 0%, transparent 60%);
    pointer-events: none;
    z-index: 0;
  }

  .lander {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 1.5rem;
    position: relative;
    z-index: 1;
  }

  /* Hero */
  .hero {
    text-align: center;
    padding: 5rem 0 3rem;
  }

  .hero-badge {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-accent-light);
    background: rgba(245, 158, 11, 0.1);
    border: 1px solid rgba(245, 158, 11, 0.2);
    padding: 0.35rem 0.75rem;
    border-radius: 100px;
    margin-bottom: 1.5rem;
  }

  .hero-title {
    font-size: clamp(2rem, 6vw, 3.5rem);
    font-weight: 700;
    line-height: 1.12;
    margin: 0 0 1.5rem;
    letter-spacing: -0.025em;
    color: var(--text-primary);
  }

  .hero-tagline {
    font-size: 1.125rem;
    color: var(--text-secondary);
    max-width: 560px;
    margin: 0 auto 2.5rem;
    line-height: 1.6;
  }

  .hero-actions {
    display: flex;
    gap: 0.75rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 2.5rem;
  }

  .button {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.625rem 1.25rem;
    border-radius: 8px;
    font-size: 0.875rem;
    font-weight: 500;
    text-decoration: none;
    transition: all 0.15s ease;
  }

  .button.primary {
    background: var(--color-accent);
    color: #0a0a0f;
  }

  .button.primary:hover {
    background: var(--color-accent-light);
    transform: translateY(-1px);
  }

  .button.secondary {
    background: var(--surface-2);
    color: var(--text-secondary);
    border: 1px solid var(--border-default);
  }

  .button.secondary:hover {
    background: var(--surface-3);
    border-color: rgba(255, 255, 255, 0.18);
  }

  .install-command {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: var(--surface-1);
    border: 1px solid var(--border-subtle);
    border-radius: 8px;
    padding: 0.625rem 1rem;
    font-family: var(--sl-font-mono);
    font-size: 0.875rem;
  }

  .install-prompt {
    color: var(--color-accent);
  }

  .install-command code {
    background: none;
    padding: 0;
    color: var(--text-secondary);
  }

  .copy-button {
    background: none;
    border: none;
    color: var(--text-muted);
    cursor: pointer;
    padding: 0.25rem;
    display: flex;
    transition: color 0.15s;
    margin-left: 0.25rem;
  }

  .copy-button:hover { color: var(--text-primary); }
  .copy-button.copied { color: var(--color-success); }

  /* Section Headers */
  .section-header {
    text-align: center;
    margin-bottom: 2.5rem;
  }

  .section-label {
    display: inline-block;
    font-size: 0.7rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-accent-light);
    margin-bottom: 0.5rem;
  }

  .section-title {
    font-size: 1.75rem;
    font-weight: 600;
    margin: 0;
    color: var(--text-primary);
  }

  .section-desc {
    color: var(--text-secondary);
    margin: 0.5rem 0 0;
    font-size: 1rem;
  }

  /* Lifecycle Section */
  .lifecycle {
    padding: 3rem 0;
  }

  .phase-flow {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1rem;
    align-items: stretch;
  }

  .phase-card {
    background: var(--surface-1);
    border: 1px solid var(--border-subtle);
    border-radius: 12px;
    padding: 1.25rem 0.75rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 0.5rem;
    transition: all 0.2s;
    position: relative;
  }

  .phase-card:hover {
    background: var(--surface-2);
    border-color: rgba(245, 158, 11, 0.3);
    transform: translateY(-2px);
  }

  .phase-number {
    width: 28px;
    height: 28px;
    background: rgba(245, 158, 11, 0.15);
    border: 1px solid rgba(245, 158, 11, 0.3);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.75rem;
    font-weight: 700;
    color: var(--color-accent-light);
  }

  .phase-name {
    font-size: 0.95rem;
    font-weight: 600;
    margin: 0;
    color: var(--text-primary);
  }

  .phase-desc {
    font-size: 0.75rem;
    color: var(--text-muted);
    margin: 0;
    line-height: 1.4;
  }

  .phase-detail-link {
    text-align: center;
    margin-top: 1.5rem;
  }

  .phase-detail-link a {
    color: var(--color-accent-light);
    text-decoration: none;
    font-size: 0.9rem;
    transition: color 0.15s;
  }

  .phase-detail-link a:hover {
    color: var(--color-accent);
    text-decoration: underline;
  }

  @media (max-width: 700px) {
    .phase-flow {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  /* Problem Section */
  .problem {
    padding: 3rem 0;
  }

  .problem-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 1rem;
  }

  .problem-card {
    background: var(--surface-1);
    border: 1px solid var(--border-subtle);
    border-radius: 12px;
    padding: 1.25rem;
    transition: all 0.2s;
  }

  .problem-card:hover {
    background: var(--surface-2);
    border-color: var(--border-default);
  }

  .problem-icon {
    width: 36px;
    height: 36px;
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 0.75rem;
  }

  .problem-icon.bad {
    background: rgba(239, 68, 68, 0.12);
    color: var(--color-red);
  }

  .problem-card h3 {
    font-size: 0.95rem;
    font-weight: 600;
    margin: 0 0 0.375rem;
    color: var(--text-primary);
  }

  .problem-card p {
    font-size: 0.85rem;
    color: var(--text-secondary);
    margin: 0;
    line-height: 1.5;
  }

  /* Discipline Section */
  .disciplines {
    padding: 3rem 0;
  }

  .discipline-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1rem;
  }

  .discipline-card {
    background: var(--surface-1);
    border: 1px solid var(--border-subtle);
    border-radius: 12px;
    padding: 1.25rem;
    transition: all 0.2s;
  }

  .discipline-card:hover {
    background: var(--surface-2);
    border-color: var(--border-default);
    transform: translateY(-2px);
  }

  .discipline-icon {
    width: 40px;
    height: 40px;
    background: rgba(245, 158, 11, 0.1);
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-accent-light);
    margin-bottom: 1rem;
  }

  .discipline-card h3 {
    font-size: 1rem;
    font-weight: 600;
    margin: 0 0 0.5rem;
    color: var(--text-primary);
  }

  .discipline-card p {
    font-size: 0.875rem;
    color: var(--text-secondary);
    margin: 0;
    line-height: 1.5;
  }

  /* Compatibility */
  .compatibility {
    padding: 3rem 0;
  }

  .client-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    justify-content: center;
  }

  .client-item {
    background: var(--surface-1);
    border: 1px solid var(--border-subtle);
    border-radius: 100px;
    padding: 0.5rem 1.25rem;
    font-size: 0.85rem;
    font-weight: 500;
    color: var(--text-secondary);
    transition: all 0.15s;
  }

  .client-item:hover {
    background: var(--surface-2);
    border-color: var(--border-default);
  }

  /* Specs Anywhere */
  .specs-anywhere {
    padding: 3rem 0;
  }

  .spec-options {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1rem;
  }

  .spec-card {
    background: var(--surface-1);
    border: 1px solid var(--border-subtle);
    border-radius: 12px;
    padding: 1.5rem;
    transition: all 0.2s;
  }

  .spec-card:hover {
    background: var(--surface-2);
    border-color: var(--border-default);
  }

  .spec-path {
    display: inline-block;
    font-size: 0.85rem;
    background: rgba(245, 158, 11, 0.08);
    border: 1px solid rgba(245, 158, 11, 0.15);
    color: var(--color-accent);
    padding: 0.25rem 0.625rem;
    border-radius: 6px;
    margin-bottom: 0.75rem;
  }

  .spec-card p {
    font-size: 0.875rem;
    color: var(--text-secondary);
    margin: 0;
    line-height: 1.5;
  }

  .spec-card a {
    color: var(--color-accent-light);
    text-decoration: none;
  }

  .spec-card a:hover {
    text-decoration: underline;
  }

  /* CTA */
  .cta {
    padding: 4rem 0;
  }

  .cta-content {
    text-align: center;
    background: linear-gradient(135deg, rgba(245, 158, 11, 0.08), rgba(245, 158, 11, 0.02));
    border: 1px solid rgba(245, 158, 11, 0.15);
    border-radius: 16px;
    padding: 3rem 2rem;
  }

  .cta-title {
    font-size: 1.75rem;
    font-weight: 700;
    margin: 0 0 0.75rem;
    color: var(--text-primary);
  }

  .cta-desc {
    color: var(--text-secondary);
    margin: 0 0 1.5rem;
    font-size: 1rem;
  }

  .cta-install {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: rgba(0, 0, 0, 0.3);
    border: 1px solid var(--border-subtle);
    border-radius: 8px;
    padding: 0.625rem 1.25rem;
    font-family: var(--sl-font-mono);
    font-size: 0.875rem;
    margin-bottom: 1.5rem;
  }

  .cta-install code {
    background: none;
    padding: 0;
    color: var(--text-secondary);
  }

  .cta-actions {
    display: flex;
    gap: 0.75rem;
    justify-content: center;
  }

  /* Footer */
  .lander-footer {
    padding: 2rem 0;
    border-top: 1px solid var(--border-subtle);
  }

  .footer-content {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    font-size: 0.8rem;
    color: var(--text-muted);
  }

  .footer-brand {
    font-weight: 600;
    color: var(--text-secondary);
  }

  .footer-link {
    color: var(--text-muted);
    text-decoration: none;
  }

  .footer-link:hover {
    color: var(--text-secondary);
  }
</style>
